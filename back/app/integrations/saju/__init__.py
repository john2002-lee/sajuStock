"""사주 엔진 통합 경계 (통합 기획 5.3).

기획이 이 경계를 요구한 이유는 그때 사주 엔진이 **다른 사람이 다른 스택으로** 만들고
있었기 때문이다 — 파이썬 라이브러리든 별도 HTTP 서비스든 경계 파일 하나로 격리해
두면 앱이 흔들리지 않는다는 것이었다.

합치고 나서도 경계를 유지한다. 지금은 `client.py` 가 같은 저장소의
`domain/saju/` 를 부르는 얇은 층이지만, 그 얇음이 곧 **판단 경로가 사주를 모른다**는
사실의 물리적 형태다: `services/advice_service.py` 는 `InvestorProfile` 숫자만 보고,
사주 원문이 그쪽으로 넘어가는 유일한 통로가 여기라 감시할 곳이 한 곳이다.
"""
